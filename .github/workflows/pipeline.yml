name: Lint and Test
run-name: Run Lint and Tests of ${{ github.repository }} 🚀
on: 
  push:
    branches:
      - main

jobs:
  Lint:
  
    runs-on: ubuntu-latest

    steps:
      - name: List files
        run: ls -R

      - uses: ./.github/actions/nodejs-setup

      - name: Run Eslint
        run: npx eslint .
  Test:
    runs-on: ubuntu-latest

    steps:
      - uses: ./.github/actions/nodejs-setup

      - name: Run Tests
        run: npm run test