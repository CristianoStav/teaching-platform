name: Lint and Test
run-name: Run Lint and Tests of ${{ github.repository }} 🚀
on: [push]

jobs:
  Lint:
    runs-on: ubuntu-latest

    steps:
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
      - run: npx eslint .